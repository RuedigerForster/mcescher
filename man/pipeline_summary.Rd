% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise.R
\name{pipeline_summary}
\alias{pipeline_summary}
\title{Run and summarise the full downstream pipeline for a set of chromatograms.}
\usage{
pipeline_summary(
  signal_mat,
  baseline_mat,
  RT,
  signal_mat_comp = NULL,
  RT_comp = NULL,
  baseline_mat_comp = NULL,
  signal_presass_mat = NULL,
  atsa_result = NULL,
  inh = NULL,
  RMSE_pred = NULL,
  smooth_width = 11L,
  amp_thresh = 0,
  slope_thresh = 0,
  integrate_methods = c("PD", "TS", "gauss", "EGH"),
  run_ensemble = TRUE,
  ensemble_configs = NULL,
  ensemble_method = "TS",
  method_lod = NULL,
  method_loq = NULL,
  cal_slope = NULL,
  noise_method = "mad",
  col_names = NULL,
  method_config = NULL,
  verbose = TRUE
)
}
\arguments{
\item{signal_mat}{n_samples  n_chroms matrix (aligned signal, e.g. df2).}

\item{baseline_mat}{n_samples  n_chroms matrix (fitted baseline, e.g. df3).}

\item{RT}{Retention time vector in minutes (length = n_samples).}

\item{signal_mat_comp}{Optional compressed signal matrix (block-averaged).
Speeds up the ensemble step when provided.}

\item{RT_comp}{Retention time vector matching \code{signal_mat_comp}.}

\item{baseline_mat_comp}{Optional compressed baseline matrix.}

\item{signal_presass_mat}{Optional pre-SASS signal matrix (same dims as
\code{signal_mat}) used to compute \code{u_sass}.}

\item{atsa_result}{List returned by atsa(). Optional; used for alignment stats.}

\item{inh}{List returned by inhibit(). Optional; used to report regions.}

\item{RMSE_pred}{n_samples  n_chroms RMSE matrix from read_chroma.R. Optional.}

\item{smooth_width}{Smoothing window (samples) for peak detection.}

\item{amp_thresh}{Minimum peak height for detection.}

\item{slope_thresh}{Minimum derivative slope for peak detection.}

\item{integrate_methods}{Character vector: any of "PD","TS","gauss","EGH".}

\item{run_ensemble}{Logical. Run baseline ensemble to estimate area uncertainty.
Slow (~12 baselines  n_chroms). Set FALSE to skip.}

\item{ensemble_configs}{Configuration list; default_ensemble_configs() if NULL.}

\item{ensemble_method}{Integration method used inside the ensemble ("TS").}

\item{method_lod}{Method LOD in signal units for comparison. NULL = skip.}

\item{method_loq}{Method LOQ in signal units. NULL = skip.}

\item{cal_slope}{Calibration slope (signal / conc unit). NULL = skip.}

\item{noise_method}{Noise estimator: "mad", "sd", or "rms".}

\item{col_names}{Optional character vector of column labels.}

\item{method_config}{List returned by \code{read_method_config()}.
Supplies peak names, area scaling, and per-peak ensemble overrides.
If NULL the function runs without peak naming or area scaling.}

\item{verbose}{Print section headers while running.}
}
\value{
Named list:
$data_info      scalar summary of inputs
$alignment      per-column alignment stats (if atsa_result given)
$baseline_rmse  per-column RMSE summary (if RMSE_pred given)
$peaks          list of per-column peak data frames
$integration    list of per-column integration data frames
$uncertainty    list of per-column ensemble summary data frames
$lod_loq        data frame, one row per column
$report         character vector of printed report lines (invisible)
}
\description{
Run and summarise the full downstream pipeline for a set of chromatograms.
}
\examples{
\dontrun{
data(Grob_mix)
cfg <- read_method_config(
  system.file("extdata", "method.yaml", package = "mcescher")
)
# Grob_mix is a raw signal matrix; supply a flat zero baseline for a
# quick non-ensemble run:
RT  <- as.numeric(rownames(Grob_mix))
bl  <- matrix(0, nrow = nrow(Grob_mix), ncol = ncol(Grob_mix))
result <- pipeline_summary(
  signal_mat    = Grob_mix,
  baseline_mat  = bl,
  RT            = RT,
  method_config = cfg,
  run_ensemble  = FALSE
)
as.data.frame(result)
}
}
