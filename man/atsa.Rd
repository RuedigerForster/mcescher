% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atsa.R
\name{atsa}
\alias{atsa}
\title{Automatic Time-Shift Alignment for chromatographic matrices.}
\usage{
atsa(
  mat,
  RT,
  segment_size = 3,
  shift_max = 0.5,
  smooth_width = 11L,
  amp_thresh = 0,
  slope_thresh = 0,
  baseline_correct = TRUE,
  lmv_span = 50L,
  ref_peaks_RT = NULL,
  ref_peaks_width = 0.1,
  min_peaks = 3L,
  verbose = TRUE
)
}
\arguments{
\item{mat}{Numeric matrix, rows = time points, columns = samples.}

\item{RT}{Retention time vector in minutes (length = nrow(mat)).}

\item{segment_size}{Initial segment size for preliminary alignment (min).}

\item{shift_max}{Maximum time-shift search range (min).}

\item{smooth_width}{Smoothing window (samples) for peak detection.}

\item{amp_thresh}{Minimum peak height for detection.}

\item{slope_thresh}{Minimum derivative slope for peak detection.}

\item{baseline_correct}{Apply LMV-RSA baseline correction before alignment.}

\item{lmv_span}{Window parameter for LMV-RSA.}

\item{ref_peaks_RT}{Optional numeric vector of known retention times (min)
to use as alignment anchors instead of auto-detection.
Typically derived from the method config peak RT_ref
values.  When supplied, .find_peaks() is skipped for the
reference chromatogram; the signal maximum within
ref_peaks_width of each anchor is used as the peak
apex.  Test-column peaks (Stage 3) are still
auto-detected.}

\item{ref_peaks_width}{Half-width (min) of the search window around each
anchor when ref_peaks_RT is supplied.  Default 0.1 min.}

\item{min_peaks}{Minimum number of peaks per segment; segments with
fewer peaks are merged into a neighbour.}

\item{verbose}{Print progress messages.}
}
\value{
A list with:
\item{aligned}{Aligned matrix (same dimensions as mat).}
\item{baseline_corrected}{Baseline-corrected matrix used for alignment.}
\item{shifts}{Integer matrix (n_segments x n_columns) of preliminary shifts.}
\item{reference_idx}{Column index of the chosen reference chromatogram.}
\item{ref_peaks}{Peaks data frame for the reference chromatogram.}
\item{segments}{Segment table used for preliminary alignment.}
}
\description{
Automatic Time-Shift Alignment for chromatographic matrices.
}
